---
ontology: true
type: execute
status: completed
domain: pilot-analytics
summary: Три связанные страницы
tags: [gsd, plan]
relatedTo: [../../REQUIREMENTS.md, ../../TASKS.md]
phase: 01-pilot-dashboard
plan: 03
wave: 3
depends_on: ["01-01","01-02"]
files_modified: ["src/dashboard/App.tsx","src/dashboard/MapPage.tsx","src/dashboard/Charts.tsx","src/dashboard/styles.css"]
autonomous: true
requirements: ["UI-01","UI-02","UI-03"]
must_haves:
  truths:
    - "Данные и интерфейс соответствуют утверждённым правилам"
    - "Неизвестные значения имеют явный статус"
---

<objective>Три связанные страницы</objective>

<context>@.planning/REQUIREMENTS.md @.planning/DECISIONS.md @.planning/STATE.md</context>

<tasks>
<task type="auto" id="03-1">
<name>Главная</name>
<files>src/dashboard/App.tsx, src/dashboard/MapPage.tsx, src/dashboard/Charts.tsx, src/dashboard/styles.css</files>
<action>Шесть блоков, квартальные графики, два ряда групп, факты с переходами, свёрнутые фильтры.</action>
<verify>Каждый показатель учитывает роль и продуктовый состав; неизвестное обозначено.</verify>
<done>Каждый показатель учитывает роль и продуктовый состав; неизвестное обозначено.</done>
</task>

<task type="auto" id="03-2">
<name>Анализ</name>
<files>src/dashboard/App.tsx, src/dashboard/MapPage.tsx, src/dashboard/Charts.tsx, src/dashboard/styles.css</files>
<action>ГОСБ, продукты, стадии, сотрудники пилота, поиск и исходные строки; контекст в URL.</action>
<verify>Поиск и переходы воспроизводимы, типизация проходит.</verify>
<done>Поиск и переходы воспроизводимы, типизация проходит.</done>
</task>

<task type="auto" id="03-3">
<name>Карта и адаптивность</name>
<files>src/dashboard/App.tsx, src/dashboard/MapPage.tsx, src/dashboard/Charts.tsx, src/dashboard/styles.css</files>
<action>Реальные геометрии, выбор ГОСБ, панорамирование, масштаб, показатель и квартал; reduced motion.</action>
<verify>Значения совпадают с главной и анализом; клавиатура и мобильный экран работают.</verify>
<done>Значения совпадают с главной и анализом; клавиатура и мобильный экран работают.</done>
</task>
</tasks>

<verification>Проверить каждое условие verify по фактическим артефактам. Не отмечать незавершённые проверки выполненными.</verification>
<output>Создать 03-SUMMARY.md с результатами, ограничениями и проверками.</output>

